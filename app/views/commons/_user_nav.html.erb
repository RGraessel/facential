<div id="full_user_nav">
  <div id="user_nav_top_section">
    <div class="" id="top_space_div"></div>
    <img class="circle" id="profile_picture"/>
    <h1 id="full_user_name"><%= "#{current_user.first_name.capitalize} #{current_user.last_name.capitalize}" %></h1>
    <div class="" id="after_username_space_div"></div>
    <ul>
      <li id="completed_total"></li>
      <li id="pending_total"></li>
      <span id="completed_total_text">Something</li>
        <span id="pending_total_text">pending</li>
        </ul>
      </div>
      <br>
      <div class="list-group">
        <a href="" class="list-group-item">Paths</a>
        <%=link_to user_courses_path(current_user.id) do%>
        <div class="list-group-item">Courses</div>
        <% end %>
        <!-- user_course_topics GET    /users/:user_id/courses/:course_id/topics -->
        <%=link_to user_course_topics_path(current_user.id, @user_course ) do %>
        <div class="list-group-item">Topics</div>
        <% end %>
        <%=link_to user_course_topic_lessons_path(current_user.id, @user_course, @user_topics, @lessons) do %>
        <div class="list-group-item">Lessons</div>
        <% end %>
      </div>
    </div>
