<p id="notice"><%= notice %></p>

<p>
  <strong>Topic name:</strong>
  <%= @topic.topic_name %>
</p>

<p>
  <strong>Course:</strong>
  <%= @topic.course %>
</p>

<%= link_to 'Edit', edit_user_course_topic_path(@current_user.id, @course, @topic) %> |
<%= link_to 'Back', user_course_topics_path %>
