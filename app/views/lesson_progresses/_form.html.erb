<%= form_for(@lesson_progress) do |f| %>
  <% if @lesson_progress.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lesson_progress.errors.count, "error") %> prohibited this lesson_progress from being saved:</h2>

      <ul>
      <% @lesson_progress.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :lesson_id %><br>
    <%= f.text_field :lesson_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
